@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(NOT TARGET CUDA::nvToolsExt)
    add_library(CUDA::nvToolsExt INTERFACE IMPORTED)
endif()
find_dependency(CUDAToolkit)

include("${CMAKE_CURRENT_LIST_DIR}/ParallelMaterTargets.cmake")
